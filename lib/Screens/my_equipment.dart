import 'package:flutter/material.dart';

class MyEquipments extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('View my Equipments'),
        ),
      ),
    );
  }
}
